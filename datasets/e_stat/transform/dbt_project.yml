name: "e_stat"
version: "1.0.0"

profile: "e_stat"

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  e_stat:
    +database: e_stat
    raw:
      +materialized: view
    mart:
      +materialized: view
      municipal:
        +schema: municipal
      pref:
        +schema: pref
