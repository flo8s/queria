{{ config(materialized='view') }}

select
    "Type" as property_type,
    "Region" as region,
    "MunicipalityCode" as municipality_code,
    "Prefecture" as prefecture,
    "Municipality" as municipality,
    "DistrictName" as district_name,
    try_cast("TradePrice" as bigint) as trade_price,
    try_cast("PricePerUnit" as bigint) as price_per_unit,
    "FloorPlan" as floor_plan,
    try_cast("Area" as double) as area,
    try_cast("UnitPrice" as bigint) as unit_price,
    "LandShape" as land_shape,
    try_cast("Frontage" as double) as frontage,
    try_cast("TotalFloorArea" as double) as total_floor_area,
    "BuildingYear" as building_year,
    "Structure" as structure,
    "Use" as use_type,
    "Purpose" as purpose,
    "Direction" as direction,
    "Classification" as classification,
    try_cast("Breadth" as double) as breadth,
    "CityPlanning" as city_planning,
    try_cast("CoverageRatio" as double) as coverage_ratio,
    try_cast("FloorAreaRatio" as double) as floor_area_ratio,
    "Period" as period,
    "Renovation" as renovation,
    "Remarks" as remarks,
    "PriceCategory" as price_category,
    "DistrictCode" as district_code,
    _area_code as prefecture_code,
    _year as year,
    _quarter as quarter
from {{ ref('raw_trade_prices') }}
